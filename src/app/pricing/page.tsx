// @ts-nocheck
import PricingClient from "./PricingClient";

export const metadata = {
  title: "AIInfraWatch — Pricing",
  description:
    "The AI compute audit is free. Pay only a fraction of the savings we find: a one-time migration playbook, ongoing drift monitoring, or enterprise procurement leverage.",
  robots: "index, follow",
};

export default function Page() {
  return <PricingClient />;
}
